'use strict';

/**
 * @ngdoc function
 * @name clfApp.controller:SigninCtrl
 * @description
 * # SigninCtrl
 * Controller of the clfApp
 */
angular.module('clfApp')
  .controller('SigninCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
