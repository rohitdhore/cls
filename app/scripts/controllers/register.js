'use strict';

/**
 * @ngdoc function
 * @name clfApp.controller:RegisterCtrl
 * @description
 * # RegisterCtrl
 * Controller of the clfApp
 */
angular.module('clfApp')
  .controller('RegisterCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
